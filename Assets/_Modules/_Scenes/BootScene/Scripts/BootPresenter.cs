using Mimi.Prototypes.SceneManagement;
using Mimi.Prototypes.UI;

namespace Mimi.Prototypes
{
    public class BootPresenter : BaseScenePresenter<BootScene>
    {
        public override void Initialize(BootScene sceneController)
        {
            base.Initialize(sceneController);
        }
    }
}